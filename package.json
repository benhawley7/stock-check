{
  "name": "stock-check",
  "version": "0.0.0",
  "description": "Return the current stock levels from stock and transaction files",
  "main": "dist/app.js",
  "scripts": {
    "build": "npx --no-install tsc",
    "start": "npm run build && node dist/src/app.js",
    "test": "npm run build && npx --no-install tape dist/test/index.js"
  },
  "author": "Ben Hawley",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^14.11.1",
    "@types/sinon": "^9.0.8",
    "@types/tape": "^4.13.0",
    "sinon": "^9.2.0",
    "tape": "^5.0.1",
    "typescript": "^4.0.3"
  }
}
